
<%= form_for(@book) do |book_form| %>
  <% if @book.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
    <ul>
    <% @book.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  
  <% end %>
 
  <div class="field">
    <%= book_form.label :name %><br />
    <%= book_form.text_field :name %>
  </div>
  <div class="field">
    <%= book_form.label :description %><br />
    <%= book_form.text_field :description %>
  </div>
  <div class="field">
    <%= book_form.label :author %><br />
    <%= book_form.text_field :author %>
  </div>
  <div>
    <%= book_form.label :publisher %><br />
    <%= book_form.collection_select(:publisher_id, Publisher.all, :id, :name ) %>
</div>
  <div class="field">
    <%= book_form.label :price %><br />
    <%= book_form.text_field :price %>
  </div>
  <div class="actions">
    <%= book_form.submit %>
  </div>
<% end %>